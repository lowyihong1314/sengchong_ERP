Flask==3.0.3
Flask-SQLAlchemy>=3.1
SQLAlchemy>=2.0
alembic>=1.13
pymssql==2.3.13
Pillow
gunicorn

psycopg[binary]>=3.2

# Document inbox: .xlsx and .docx are read as text rather than
# photographed, so the model gets the exact cell values.
openpyxl
python-docx

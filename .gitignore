# --- Secrets: never commit ---
.env
dns_sync/cloudflare_API.md

# --- ERP-owned data (real customer and accounting data) ---
sqlserver-backups/
pg-backups/
var/

# --- docs/ is local only, none of it is pushed ---
# It holds customer leads, EPF receipts, scans of payslips and working notes
# about real people. Rather than deciding file by file which parts are safe,
# the whole directory stays out of the repository.
docs/

# --- Runtime / build output ---
session_data.json
session_data.json.lock
.venv/
__pycache__/
*.pyc
frontend/node_modules/
frontend/dist/
npm-debug.log*
.flask.log
*.log

# --- Local scratch ---
*.bak-*
.smoke/
.ab/
